import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    black: {
      50: '#23252b',
      900: '#000000',
    },
    white: {
      50: '#edf1f5',
      900: '#ffffff',
    },
  },
})

export default theme
